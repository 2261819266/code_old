#include <bits/stdc++.h>
using namespace std;

int main () {
	freopen("meow.in","r",stdin);
	freopen("meow.out","w",stdout);
	int t;
	cin >> t;
	while(t--) {
		int n, m, k;
		cin >> n >> m >> k;
		for (int i = 1; i<=m;i++) {
			int s;
			cin >> s;
		}
		cout <<"0\n";
	}
	return 0;
}

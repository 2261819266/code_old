#include<bits/stdc++.h>
using namespace std;
int T, n, m, k;
int q[100005], head, tail;
int poss[100005];

stack<int> s;

void dfs(){
	
}

int main(){
	cin >> T;
	while(T --){
		cin >> n >> m >> k;
		for(int i = 1; i <= m; i ++) cin >> poss[i];
		for(int i = m ; i >= 1; i --) stack.push(poss[i]);
		
	}
	return 0;
}
//52ccf

#include<bits/stdc++.h>
using namespace std;
int main()
{
    freopen("match.in","r",stdin);
	freopen("match.out","w",stdout);
	char m[100],n[100],a,b;
	int p,q;
	cin>>m>>n>>p>>q;
	for(int i=0;i++;)
	{
		if(p=1,q=2)
	{
		a+=4;
	}
	else
	    a+=2;
	for(int i=0;i<=0;i++)
	{
		a=m[i]+n[i];
		b=a/p+q*a;
	}
	cout<<b<<" "<<a;
}
	fclose(stdin);
	fclose(stdout);
	return 0;
}

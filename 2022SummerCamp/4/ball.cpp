#include <bits/stdc++.h>
using namespace std;
const int mod = 1e9 + 7;
int n, x, y;
signed main()
{
    cin >> n;
    for (int i = 1; i <= n; ++i) {
        scanf("%d%d%d", &n, &x, &y);
    }
    return 0;
}
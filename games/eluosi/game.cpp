#include <cstdio>
#include <cstring>
#include <windows.h>
#include <conio.h>
#define W 10
#define H 25
#define S (W * H)
#define pause system("pause");

// int a[W + 10][H + 10];

// void init() {
// 	SetConsoleTitle(TEXT("Alexey Pazhitnov v.0.1.0"));
// 	for (int c = 0; c <= W + 10; c++)
// 		memset(a[c], -1, sizeof(a[c]));
// 	for (int c = 1; c <= W; c++)
// 		for (int d = 1; d <= H; d++)
// 			a[c][d] = 0;
// 	return;
// }
class LimitedInt {
};

int main() {
}
